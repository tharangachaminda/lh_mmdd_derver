{
    "name": "@learning-hub/shared",
    "version": "2.0.0",
    "description": "Shared types, utilities, and constants for the Learning Hub educational platform",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage",
        "lint": "eslint src/**/*.ts",
        "clean": "rm -rf dist"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "jest": "^29.0.0",
        "@types/jest": "^29.0.0"
    },
    "files": [
        "dist",
        "package.json",
        "README.md"
    ],
    "publishConfig": {
        "access": "restricted"
    }
}
